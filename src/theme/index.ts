import { createTheme, Theme } from "@mui/material/styles";

export const theme: Theme = createTheme({
  palette: {
    text: {
      primary: "#393939",
      secondary: "#595959",
    },
  },
  shape: {
    borderRadius: 8,
  },
});
